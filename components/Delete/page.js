import { ObjectId } from "mongodb";
import { connectDB } from "../../util/database";

export default async function Delete() {

    return (
        <div>
            <button>삭제</button>
        </div>
    )
}
